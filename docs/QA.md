# Проверка SĀL Legal

Проверки разработки: 10–11 сентября 2026. Публикация и HTTP-проверка: 12 сентября 2026.

## Подтверждено

- TypeScript, production build Vite 8 и предварительный рендер: успешно.
- 12 автоматических проверок: успешно, локально и в GitHub Actions.
- Валидация формы, обязательность выбранного контакта и согласия, ограничения полей, безопасные URL, HTTP 2xx/ошибки/429, сетевой отказ и отмена проверены с заглушками транспорта.
- Проверены статические маршруты, ресурсы с base path /sal/, внутренние якоря, метаданные, отсутствие tracking/secrets и бюджеты ресурсов.
- npm audit после обновления Vite и исправления minimatch: 0 известных уязвимостей на момент проверки.
- Клиентский JavaScript: около 84 КБ gzip; CSS около 8 КБ gzip. Hero WebP: 74/192 КБ; интерьер: 25/66 КБ. Шрифты локальные, font-display: swap; размеры изображений заданы.
- Первый production workflow и отдельный процесс Pages завершились успешно для commit a5dbcd575ff42d883aa11f86abf2e901b712ade9.
- В workflow добавлена обязательная задача verify после deployment: scripts/verify-deployment.mjs проверяет публичные HTTP-ответы шести страниц, canonical, якоря, уникальность ID, связанные CSS/JS/шрифты/изображения, robots и sitemap. Ее фактический результат смотрите в последнем запуске Actions.

## Ограничения — не отмечены как пройденные

- Локальный предпросмотр был недоступен, подключение облачного браузера к опубликованному сайту не завершилось; затем рабочая среда отключилась.
- Поэтому визуальная проверка desktop/mobile, реальное отсутствие overflow, клики, keyboard navigation, 200% text/zoom, reduced motion и консоль браузера НЕ подтверждены интерактивным прогоном. Реализация этих механизмов присутствует, но проверка исходников не заменяет браузер.
- Lighthouse и физические iOS/Android-устройства не измерялись; численные баллы не заявляются.
- Реальные обращения и платежи не отправлялись без подключенных аккаунтов. Требуется контрольный запрос после активации формы и проверка hosted checkout на стороне провайдера.

Временный маршрут /_qa/ удален из исходников и публикации. Все ограничения оставлены явно, без заявления о полностью выполненном browser QA.
